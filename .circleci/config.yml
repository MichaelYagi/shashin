#executors:
#  example:
#    docker:
#      - image: 'cimg/openjdk:11.0'
orbs:
  maven: circleci/maven@3.6.0
version: 2.1
workflows:
  maven_test:
    jobs:
      - maven/test #: # checkout, build, test, and upload test results
          # executor: example
